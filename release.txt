1.7.6
/rooms now handles switching (replaces /room); /thread now handles listing (replaces /threads)
/roll D&D dice, /emote actions, @mentions + blips (/sound on by default). Hard refresh after update.
